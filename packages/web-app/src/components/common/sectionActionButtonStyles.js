// A small outlined MUI button containing its standard 24px icon is 44px wide.
// Fixing icon-only section actions to that reference width prevents custom
// composite icons from changing the button footprint.
export const MOBILE_SECTION_ACTION_BUTTON_SX = {
  width: 44,
  minWidth: 44,
  px: 0.5
};
