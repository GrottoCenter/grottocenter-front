// One size for every icon in the top bar. They sit on a single row a few pixels
// apart, where a size difference reads as one of them being broken rather than
// as a hierarchy — so it is shared here instead of being repeated, and drifting,
// in each component. NotificationMenu's icon lives outside this folder but is
// part of the same row, and imports it too.
export const APP_BAR_ICON_SIZE = 30;
